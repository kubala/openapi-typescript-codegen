const getResponseBody = (config: OpenAPIConfig, xhr: XMLHttpRequest): any => {
	if (xhr.status !== 204) {
		try {
			const contentType = xhr.getResponseHeader('Content-Type');
			if (contentType) {
				const isJSON = config.JSON_MEDIA_TYPES?.some(type => contentType.toLowerCase().startsWith(type));
				if (isJSON) {
					return JSON.parse(xhr.responseText);
				} else {
					return xhr.responseText;
				}
			}
		} catch (error) {
			console.error(error);
		}
	}
	return undefined;
};
